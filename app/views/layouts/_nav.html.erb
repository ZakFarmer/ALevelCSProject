<!-- Navbars will be rendered depending on whether the user is logged in or not -->
<% if !user_signed_in? %>
  <%= render '/layouts/nav_guest' %>
<% else %>
  <%= render '/layouts/nav_user' %>
<% end %>
